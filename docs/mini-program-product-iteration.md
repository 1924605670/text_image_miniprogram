# 小程序迭代说明

## 产品定位

当前小程序从单一文生图演示升级为“头条图文创作台”。核心用户是需要快速生产资讯类图文草稿的运营、编辑和本地生活内容创作者，目标是基于用户提供的事实材料，生成适合手机阅读的标题、导语、正文、要点、合规检查和封面图任务。

后台仍保留测试版发布工作台能力，用于记录需求、开发自测、测试结果、发布记录和产品验收记录。

## 本期目标

- 小程序首页直接进入头条图文创作，不做营销页。
- 用户填写选题、事实材料、报道角度、目标读者，并选择文章类型、篇幅和封面风格。
- 后端生成标题、备选标题、导语、正文、摘要要点、封面 brief、图片 prompt、负面 prompt、合规说明和事实复核提醒。
- 勾选生成封面图时，同步创建图片任务，前端展示图片任务状态和最近任务。
- 所有请求使用线上域名 `https://api2.hometodo.top/wximg`，本地不再依赖 8000 后端。

## 体验设计

- 首屏是工作台：顶部展示产品名称和状态提示，主体使用输入表单 + 结果预览结构。
- 表单强调事实材料输入，避免用户只填标题导致模型编造。
- 结果区按编辑工作流排序：推荐标题、导语、正文、要点、封面图提示词、合规与复核。
- UI 使用浅色资讯工具风格，信息密度高，适合反复生成和复制使用。
- 失败和兜底状态要明确提示：模型服务不稳定时返回保守草稿，并提示人工复核。

## 技术方案

- FastAPI 新增 `/api/toutiao-packages`，统一输出 `ToutiaoPackageOut` 结构。
- LLM 主模型使用 `LLM_MODEL`，兜底模型使用 `LLM_FALLBACK_MODELS`。
- 已兼容供应商返回 `text/event-stream` 但无普通 JSON 的情况。
- 所有文本模型不可用时，后端返回保守模板草稿，保证“文案 + 图片任务”主流程不断。
- 图片仍复用现有 `GenerationService` 异步队列，生成结果落盘并通过 `/api/images/{filename}` 访问。
- 小程序只维护轻量状态和展示逻辑，不在端上保存密钥。

## 测试策略

- Schema 测试：覆盖头条请求字段 trim、枚举和用户 ID 校验。
- LLM 客户端测试：覆盖 JSON 输出、SSE 空响应、超时、兜底模型和保守模板。
- API 测试：覆盖 `include_image=false` 和图片任务创建路径。
- 小程序检查：`node -c miniprogram/pages/index/index.js`。
- 线上验收：请求 `https://api2.hometodo.top/wximg/api/health`、`/api/toutiao-packages`，并轮询图片任务到 `succeeded`。

## 发布状态

仓库已包含微信小程序工程目录 `miniprogram/` 和 `project.config.json`，appid 为 `wx30321379334ec662`。本机微信开发者工具已可打开当前项目，开发阶段可通过工具上传测试版本。

后续如果要完全 CI 化上传微信测试版本，还需要补充上传私钥、机器人编号和 `miniprogram-ci` 脚本。

## 二期规划：版本验收工作台

二期目标是把一期的流程看板升级为“按版本验收”的工作台，让产品、测试和运维在提交测试版之前能判断版本是否真的可验收。

### 产品目标

- 支持按目标版本筛选需求、开发、测试、发布和验收记录。
- 每个发布单必须维护发布检查清单、服务器部署结果、小程序测试版提交结果、风险记录、遗留问题和测试入口。
- 提交测试版时要求服务器结果、小程序提交结果和发布检查清单全部完成。
- 验收驳回必须记录阻塞原因，方便后续复盘。
- 顶部提供版本验收态势：发布单数量、风险项、遗留项、验收阻塞和版本准备度。

### 设计原则

- 继续采用内部工具风格，优先信息密度、可扫描性和低学习成本。
- 版本筛选放在指标区下方，避免用户在四列看板里手动定位版本。
- 发布卡片突出“检查清单完成度”，风险和遗留问题用弱警示样式展示。
- 发布记录使用统一弹窗维护，减少卡片上零散按钮和重复输入。

### 技术实现

- `workflow_release_tasks` 增加 `release_checklist`、`risk_notes`、`known_issues`、`test_version_url` 字段。
- `workflow_acceptances` 增加 `blocker_notes` 字段。
- SQLite 启动时自动补列，兼容服务器已有数据库。
- `/api/workflow/board` 支持 `version` 查询参数，并返回可选版本列表。
- 后端集中校验提交测试版和验收驳回规则，前端只负责交互和展示。

### 测试验收

- Schema/Service 覆盖检查清单校验、版本筛选和验收驳回阻塞原因。
- Store 覆盖新增字段读写和 SQLite 兼容迁移。
- API 覆盖完整 happy path 与版本筛选返回。
- 前端本地浏览器检查工作台加载、版本筛选、发布记录弹窗和提交测试版按钮。

## 三期规划：真实微信测试版发布链路

三期目标是从“记录测试版提交结果”升级为“系统可触发并追踪真实微信测试版上传”。这部分依赖小程序项目目录、appid、上传私钥或 CI 机器人配置，当前仓库仍缺少这些生产要素，因此三期先完成设计和接口边界规划，不在二期硬编码未知项目。

### 产品目标

- 接入真实小程序工程和 `miniprogram-ci`，由发布单触发测试版上传。
- 上传后自动回填上传结果、版本描述、体验二维码或测试入口。
- 支持上传失败重试、失败原因归档和回滚方案确认。
- 增加多项目、多角色权限：产品、开发、测试、运维、管理员。
- 增加通知和审计：测试版提交、验收驳回、风险未关闭时通知负责人。

### 技术方案

- 新增微信上传配置：appid、项目路径、机器人编号、私钥路径、上传描述模板。
- 使用独立上传服务封装 `miniprogram-ci`，避免发布流直接依赖命令行细节。
- 上传任务异步执行并落库，保留 stdout/stderr 摘要、退出码、二维码产物路径。
- CI/CD 继续只更新后台服务，不覆盖服务器 `.env.local`、数据目录和其他服务。
- 权限和审计独立建表，避免和当前轻量 workflow 表强耦合。

### 里程碑

- 3.1：补齐小程序工程定位、appid、上传私钥和本地 dry-run 上传脚本。
- 3.2：后端上传任务服务和发布单一键上传接口。
- 3.3：前端上传状态、失败重试、二维码预览和通知记录。
- 3.4：多项目权限、审计日志和发布复盘报表。
